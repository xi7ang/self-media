name: 'Self Media Notify'
description: 'Notification action for self-media repository'
author: 'mswnlz'
inputs:
  message:
    description: 'The message to print'
    required: false
    default: 'Hello from Self Media Action!'
runs:
  using: 'node20'
  main: 'index.js'
